[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
# Development tools
pytest = "*"
pytest-flask = "*"
black = "*"
flake8 = "*"
pre-commit = "*"

[packages]
# Core Flask
flask = "*"
flask-login = "*"
flask-sqlalchemy = "==3.1.1"
flask-migrate = "==4.1.0"
flask-cors = "==6.0.1"
flask-admin = "*"
flask-jwt-extended = "==4.7.1"
flask-security-too = "*"
# API Documentation (choose one - Flask-RESTX is better)
flask-restx = "*"
# Remove these conflicting packages:
# flask-swagger = "==0.2.14"  # Conflicts with Flask-RESTX
# flasgger = "*"  # Conflicts with Flask-RESTX
# Database
sqlalchemy = "==2.0.42"
alembic = "==1.16.4"
# Security & Authentication
bcrypt = "*"
wtforms = "==3.0.1"
# Utilities
python-dotenv = "==1.1.1"
requests = "*"
pyyaml = "==6.0.2"
# Production server
gunicorn = "==23.0.0"
# Optional: Email validation for Flask-Security
email-validator = "*"

[requires]
python_version = "3.13"

[scripts]
start = "python3 app.py"
dev = "FLASK_DEBUG=1 python3 app.py"
migrate = "flask db migrate"
upgrade = "flask db upgrade"
init-db = "flask db init"
db-init = "../devOps/quick_db.sh init"
db-data = "../devOps/quick_db.sh add-data"
db-users = "../devOps/quick_db.sh create-users"
fullstack = "../devOps/start_fullstack.sh"
# Additional useful scripts
install = "pipenv install"
install-dev = "pipenv install --dev"
shell = "pipenv shell"
format = "black ."
lint = "flake8 ."
test = "pytest"
